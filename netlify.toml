# Netlify configuration for Muckly (Next.js App Router)
[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Enable Next.js runtime logs and fine-tuning
  NEXT_FORCE_EDGE_IMAGES = "true"
